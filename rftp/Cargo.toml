[package]
name = "rftp"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.1", features = ["derive"] }
rftp-client = { path = "../rftp-client" }
rftp-server = { path = "../rftp-server" }
